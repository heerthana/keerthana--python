n=input()
l=[]
for i in range(0,len(n)):
    l.append(n[i])
le=len(l)
for i in range(0,le):
    if i <le-1:
        for j in range(i+1,le):
            if j<len(l):
                if l[i]==l[j]:
                    l.pop(j)
print(len(l))
